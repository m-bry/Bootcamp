function oppositeOdie(num) {
    // garfield reference
    var result = 0;
    // initialize variable to store data
    result = (num*(-1));
    // multiply number by (-1), store following data in var result.
    return result;
    // resturn result variable
}