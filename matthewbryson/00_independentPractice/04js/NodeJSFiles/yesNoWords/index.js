// import our package
const yesNoWords = require('yes-no-words');

let wordArray = [];
// create empty variable
for(let i = 0; i< 5; i++){
    wordArray.push(yesNo.yesRandom());
}
// iterate push method
console.log(wordArray);

//how do we get 5 random yes words BUT
// we store them in an array for later use?
// push method
// create array